import 'package:flutter/material.dart';

class UsersEmpty extends StatelessWidget {
  const UsersEmpty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.people, size: 64);
  }
}
